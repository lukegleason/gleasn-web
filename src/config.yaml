site:
  name: gleasn
  site: 'https://gleasn.com'
  base: '/'
  trailingSlash: false

  googleSiteVerificationId: OEX0_smYlsewCcc6z8tYaUMysDUgnuZXALznK75-SGw

# Default SEO metadata
metadata:
  title:
    default: gleasn
    template: '%s — gleasn.com'
  description: "\U0001F680 Thumbnail Designer Landing Page"
  robots:
    index: true
    follow: true
  openGraph:
    site_name: gleasn.com
    images:
        url: '/assets/images/default.png'
        width: 1200
        height: 628
    type: website
  twitter:
    handle: '@gleasn'
    site: '@gleasn'
    cardType: summary_large_image

i18n:
  language: en
  textDirection: ltr

apps:
  blog:
    isEnabled: true
    postsPerPage: 6

    post:
      isEnabled: true
      permalink: '/%slug%' # Variables: %slug%, %year%, %month%, %day%, %hour%, %minute%, %second%, %category%
      robots:
        index: true

    list:
      isEnabled: true
      pathname: 'blog' # Blog main path, you can change this to "articles" (/articles)
      robots:
        index: true

    category:
      isEnabled: true
      pathname: 'category' # Category main path /category/some-category, you can change this to "group" (/group/some-category)
      robots:
        index: true

    tag:
      isEnabled: false
      pathname: 'tag' # Tag main path /tag/some-tag, you can change this to "topics" (/topics/some-category)
      robots:
        index: false

    isRelatedPostsEnabled: true
    relatedPostsCount: 4

analytics:
  vendors:
    googleAnalytics:
      id: null # or "G-XXXXXXXXXX"

ui:
  theme: 'light' # Values: "system" | "light" | "dark" | "light:only" | "dark:only"
